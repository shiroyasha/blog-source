---
blog: blog
---

<div class="lander">
  <ul>
    <% page_articles.each_with_index do |article, index| %>
      <li>
        <div class="counter">
          <%= page_articles.count - index %>
        </div>

        <div class="header">
          <time><%= article.date.strftime("%B %e, %Y") %></time>

          <% if article.data[:external] == true %>
            <%= link_to article.title, article.data[:link] %>
          <% else %>
            <%= link_to article.title, article %>
          <% end %>
        </div>
      </li>
    <% end %>
  </ol>
</div>
